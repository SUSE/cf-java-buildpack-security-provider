---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: openjdk
    tag: 7-jdk-alpine

inputs:
- name: java-buildpack-security-provider
outputs:
- name: output

caches:
- path: maven

run:
  path: java-buildpack-security-provider/ci/deploy.sh
